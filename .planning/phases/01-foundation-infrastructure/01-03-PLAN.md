---
phase: 01-foundation-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [supabase/migrations/20260511000000_matchmaking.sql, lib/features/auth/services/auth_service.dart, lib/main.dart]
autonomous: true
requirements: [1.3, 2.2]

must_haves:
  truths:
    - "Supabase database schema for matchmaking and matches is applied"
    - "Anonymous authentication is functional"
    - "App initializes Supabase and Isar on startup"
  artifacts:
    - path: "lib/features/auth/services/auth_service.dart"
      provides: "Authentication logic"
    - path: "lib/main.dart"
      provides: "Application entry point with init"
  key_links:
    - from: "lib/main.dart"
      to: "Supabase.initialize"
      via: "Method call"
    - from: "lib/features/auth/services/auth_service.dart"
      to: "supabaseClientProvider"
      via: "ref.watch"
---

<objective>
Configure the Supabase backend schema and implement the core authentication service to support online features.

Purpose: Enable real-time multiplayer capabilities and user identity management.
Output: Supabase migrations, AuthService implementation, and updated main.dart bootstrapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase1/RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Database Migration</name>
  <files>supabase/migrations/20260511000000_matchmaking.sql</files>
  <action>
    Create a SQL migration file to set up the matchmaking system.
    Tables:
    - `matchmaking_queue` (player_id uuid pk, created_at)
    - `matches` (id, player_x, player_o, status, board jsonb, current_turn, winner, created_at)
    
    Include the `join_matchmaking()` RPC function as defined in RESEARCH.md.
    Apply the migration using the `supabase` CLI if available, or document the SQL for manual execution in the Supabase Dashboard.
  </action>
  <verify>
    <automated>ls supabase/migrations/*.sql</automated>
  </verify>
  <done>SQL schema defined and migration file created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AuthService</name>
  <files>lib/features/auth/services/auth_service.dart</files>
  <action>
    Create an `AuthService` using `riverpod_generator`.
    Methods:
    - `signInAnonymously()`: Uses `supabase.auth.signInAnonymously()`.
    - `signOut()`: Uses `supabase.auth.signOut()`.
    - `authStateChanges()`: Stream of `AuthState`.
    
    The service should be a `Notifier` that tracks the current `User?`.
  </action>
  <verify>
    <automated>grep "signInAnonymously" lib/features/auth/services/auth_service.dart</automated>
  </verify>
  <done>AuthService implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Application Initialization</name>
  <files>lib/main.dart</files>
  <action>
    Update `main.dart` to:
    1. Ensure `WidgetsFlutterBinding.ensureInitialized()`.
    2. Initialize `Supabase` with `EnvConfig.supabaseUrl` and `EnvConfig.supabaseAnonKey`.
    3. Initialize `Isar` with the defined schemas.
    4. Wrap the `MyApp` widget with `ProviderScope`.
    5. Handle async initialization gracefully (e.g., show a loading screen or splash).
  </action>
  <verify>
    <automated>grep "Supabase.initialize" lib/main.dart</automated>
  </verify>
  <done>Main entry point updated with infrastructure initialization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → Supabase Auth | Authentication credentials/tokens |
| App → Supabase DB | Data crossing via RLS-protected API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of Privilege | Supabase DB | mitigate | Use Row Level Security (RLS) on matchmaking_queue and matches tables |
| T-01-03-02 | Spoofing | Auth Service | mitigate | Anonymous auth provides a unique ID but limited trust; session persistence handled by Supabase SDK |
</threat_model>

<verification>
Ensure the app builds and runs, reaching the entry point and connecting to Supabase.
</verification>

<success_criteria>
Remote infrastructure is configured and the app can authenticate users anonymously.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md
</output>
