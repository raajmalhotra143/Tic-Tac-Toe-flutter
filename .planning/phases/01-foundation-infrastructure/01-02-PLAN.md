---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [lib/features/progression/models/stage_progress.dart, lib/features/progression/models/settings.dart, lib/shared/providers/infrastructure_providers.dart, test/infrastructure_test.dart]
autonomous: true
requirements: [1.2, 2.1, 2.2]

must_haves:
  truths:
    - "Isar models for stage progression and user settings are defined"
    - "Isar code generation is completed without errors"
    - "Infrastructure providers (Supabase, Isar, Connectivity) are available via Riverpod"
    - "Infrastructure initialization is verified via automated tests"
  artifacts:
    - path: "lib/features/progression/models/stage_progress.dart"
      provides: "Stage progression schema"
    - path: "lib/features/progression/models/settings.dart"
      provides: "User settings schema"
    - path: "lib/shared/providers/infrastructure_providers.dart"
      provides: "Global infra clients"
    - path: "test/infrastructure_test.dart"
      provides: "Verification of DI and DB initialization"
  key_links:
    - from: "lib/shared/providers/infrastructure_providers.dart"
      to: "isar"
      via: "Isar.open"
    - from: "lib/shared/providers/infrastructure_providers.dart"
      to: "Supabase"
      via: "Supabase.instance.client"
---

<objective>
Implement local persistence models with Isar, expose infrastructure clients via Riverpod providers, and verify the setup with tests.

Purpose: Provide the app with local storage capabilities and a unified way to access external services, ensuring reliability via early testing.
Output: Isar schemas, generated code, infrastructure providers, and a foundational infrastructure test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase1/RESEARCH.md
@.planning/phases/phase1/VERIFICATION.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Isar Schemas</name>
  <files>lib/features/progression/models/stage_progress.dart, lib/features/progression/models/settings.dart</files>
  <action>
    Create Isar collection models:
    - `StageProgress`: fields [id, stageNumber (int, unique), stars (int), isUnlocked (bool), isCompleted (bool)].
    - `Settings`: fields [id, isDarkMode (bool), musicVolume (double), sfxVolume (double), selectedEmojiPack (String)].
    Use `@Collection()` annotation from `isar_community`.
  </action>
  <verify>
    <automated>grep "@Collection()" lib/features/progression/models/*.dart</automated>
  </verify>
  <done>Isar models defined.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Isar Code</name>
  <files>lib/features/progression/models/stage_progress.g.dart, lib/features/progression/models/settings.g.dart</files>
  <action>
    Run the build_runner to generate Isar schema files.
    Command: `dart run build_runner build --delete-conflicting-outputs`
  </action>
  <verify>
    <automated>ls lib/features/progression/models/*.g.dart</automated>
  </verify>
  <done>Isar code generation completed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Infrastructure Providers</name>
  <files>lib/shared/providers/infrastructure_providers.dart</files>
  <action>
    Create Riverpod providers for:
    - `supabaseClientProvider`: returns `Supabase.instance.client`.
    - `isarProvider`: An `AsyncValue` or `Provider` that provides the opened `Isar` instance.
    - `connectivityProvider`: A `StreamProvider` using `Connectivity().onConnectivityChanged`.
    Use `riverpod_generator` (@riverpod annotation) for these providers.
    Run `dart run build_runner build` again to generate provider files.
  </action>
  <verify>
    <automated>ls lib/shared/providers/*.g.dart</automated>
  </verify>
  <done>Infrastructure providers implemented and generated.</done>
</task>

<task type="auto">
  <name>Task 4: Create Infrastructure Test</name>
  <files>test/infrastructure_test.dart</files>
  <action>
    Create `test/infrastructure_test.dart` to verify the dependency injection setup.
    The test should:
    1. Verify that `ProviderScope` correctly overrides or provides the `supabaseClientProvider`.
    2. (Optional/Mocked) Verify `isarProvider` can be initialized.
    3. Ensure no initialization exceptions occur when accessing the providers.
  </action>
  <verify>
    <automated>flutter test test/infrastructure_test.dart</automated>
  </verify>
  <done>Infrastructure initialization verified via tests (Nyquist Rule met).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App → Isar | Local filesystem storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Isar Storage | accept | Local storage is on-device; standard platform sandboxing applies |
</threat_model>

<verification>
Ensure Isar schemas generate correctly and the new infrastructure test passes.
</verification>

<success_criteria>
Persistence models are ready, and the infrastructure backbone is verified through automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`
</output>
