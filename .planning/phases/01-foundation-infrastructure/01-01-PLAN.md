---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/shared/config/env_config.dart]
autonomous: true
requirements: [2.1, 2.2, 3]

must_haves:
  truths:
    - "Project dependencies are updated to the new Supabase/Riverpod stack"
    - "Directory structure follows the feature-first pattern"
    - "Environment configuration is defined for Supabase credentials"
  artifacts:
    - path: "pubspec.yaml"
      provides: "New stack dependencies"
    - path: "lib/shared/config/env_config.dart"
      provides: "Env var access"
  key_links: []
---

<objective>
Update project dependencies to the new Supabase/Riverpod/Isar stack and scaffold the feature-first directory structure.

Purpose: Establish the technical foundation and project organization for the refactored Neon Tic-Tac-Toe Pro.
Output: Updated pubspec.yaml, established directory tree, and environment configuration class.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <action>
    Update pubspec.yaml to remove legacy Firebase and Provider dependencies. 
    Add the following core dependencies:
    - supabase_flutter: ^2.12.4
    - flutter_riverpod: ^2.5.1
    - riverpod_annotation: ^2.3.5
    - isar_community: ^3.3.2
    - isar_flutter_libs: ^3.3.2
    - path_provider: ^2.1.3
    - connectivity_plus: ^6.1.0
    - google_fonts: ^8.0.2
    - go_router: ^17.1.0
    - json_annotation: ^4.9.0

    Add the following dev_dependencies:
    - riverpod_generator: ^2.4.0
    - isar_generator: ^3.3.2
    - build_runner: ^2.4.9
    - json_serializable: ^6.8.0
    - custom_lint: any
    - riverpod_lint: any

    Run `flutter pub get` after modification.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>Dependencies updated and resolved successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Directory Structure</name>
  <files>lib/features/, lib/shared/</files>
  <action>
    Create the feature-first directory structure as recommended in RESEARCH.md:
    - lib/features/auth/
    - lib/features/game/
    - lib/features/matchmaking/
    - lib/features/progression/
    - lib/shared/config/
    - lib/shared/providers/
    - lib/shared/theme/
    - lib/shared/widgets/
    - lib/shared/models/
  </action>
  <verify>
    <automated>ls -R lib/features lib/shared</automated>
  </verify>
  <done>Directory structure established.</done>
</task>

<task type="auto">
  <name>Task 3: Implement EnvConfig</name>
  <files>lib/shared/config/env_config.dart, .env.example</files>
  <action>
    Create `lib/shared/config/env_config.dart` to handle Supabase credentials using `String.fromEnvironment`.
    Include keys for `SUPABASE_URL` and `SUPABASE_ANON_KEY`.
    Also create a `.env.example` file in the root with placeholders for these values.
  </action>
  <verify>
    <automated>grep "static const supabaseUrl" lib/shared/config/env_config.dart</automated>
  </verify>
  <done>Environment configuration class created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App → Env Vars | Environment variables injected at build time |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | env_config.dart | mitigate | Use String.fromEnvironment to avoid hardcoding keys; use .gitignore for real .env files |
</threat_model>

<verification>
Ensure all directories exist and `flutter pub get` passes.
</verification>

<success_criteria>
Project is ready for infrastructure implementation with all necessary libraries and folders.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
</output>
